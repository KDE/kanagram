TODO items for Kanagram:

* investigate m_vocabSettings need
* find a way to detect language change in loadSettings? and tell the api to reload, and refresh the view, etc.
* Make changing languages switch vocab and word to use the new language
* Fix vocab editor ui issues (disable save after save, etc.)
* Fix hint fontsize for short hints
* clean up and simplify code (even though it's just a game)
* Add Apidox comments all over
* Test KNS downloads files with the right extension when support is put in for that
* Test kvtml location change
* Make engine work with unicode if kvtml reader in libkdeedu supports reading it

